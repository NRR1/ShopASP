@* @model ShopASP.Web.Models.ProductListViewModel *@
@model ShopASP.Web.Models.OrderListViewModel
@{
    ViewData["Title"] = "Список заказов";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>Название</th>
            <th>Описание</th>
            <th>Цена</th>
            <th>Дата доставки</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model.Orders){
            <tr>
                @* <td>@Html.DisplayFor(modelItem => item.)</td>
                <td>@Html.DisplayFor(modelItem => item.pDescription)</td>
                <td>@Html.DisplayFor(modelItem => item.pCost)</td>
                <td>@Html.DisplayFor(modelItem => item.pOrderDate)</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.pID">Order details</a>
                </td> *@
            </tr>
        }
    </tbody>
</table>